# GNPR-SID 项目流程与缺失项说明

本文根据仓库根目录 [Readme.md](F:\jjy\GNPR\GNPR-SID0608\Readme.md) 和数据处理说明 [Reademe.md](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\Reademe.md) 进行整理，并结合 `V2` 目录中的实际代码，对项目流程、实现方法和当前缺失内容做一次检查。

## 1. 项目目标

这个项目实现的是一个基于 Semantic ID 的生成式下一 POI 推荐流程。整体思路可以概括为：

1. 先从原始签到/轨迹数据中整理 POI 元数据和训练样本。
2. 为每个 POI 构造语义向量表示。
3. 用量化模型把 POI 语义向量编码为离散的 SID。
4. 把 SID 融入到 LLM 训练数据中，做生成式推荐模型训练。

仓库里目前保留的是 `V2` 主线，`V1` 是论文原始版本，当前不在工作区中。

## 2. 仓库模块划分

`V2` 目录可以分成 4 个部分：

1. `dataprocess/`
   负责原始数据预处理、样本切分、构造 LLM 训练数据。

2. `POIembedding/`
   负责把 POI 的类别、空间位置、时间访问分布等特征编码成连续向量。

3. `SID/`
   负责训练量化模型，把 POI 向量映射成离散 Semantic ID。

4. `LLM/`
   负责 SID 对齐、SFT 微调、LoRA 合并等 LLM 训练流程。

## 3. 推荐的完整流程

结合 README 和代码，比较合理的实际流程如下。

### 第一步：准备原始数据

当前仓库支持两种数据准备路线：

1. `sessionSplit`
   把连续时间段内的用户交互作为一个 session，一个 session 构成一个样本。
   对应 notebook：
   - [raw_preprocess.ipynb](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\sessionSplit\raw_preprocess.ipynb)
   - [lbsn_preprocess.ipynb](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\sessionSplit\lbsn_preprocess.ipynb)

2. `LLM4POI_data`
   直接复用 LLM4POI 的预处理数据，再接本项目的 SID 和 LLM 流程。
   对应 notebook：
   - [dataprocess.ipynb](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\LLM4POI_data\dataprocess.ipynb)
   - [llm_dataprocess.ipynb](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\LLM4POI_data\llm_dataprocess.ipynb)

README 还说明实验中会拼接最近 50 条历史记录，所以最终样本不是单独当前 session，而是“当前交互 + 最近 50 条历史”。

### 第二步：构造 POI 元信息表

从数据流程描述看，后续模块至少需要一个包含下列字段的 POI 级表格：

- `pid`
- `category`
- `latitude`
- `longitude`
- `visit_time_and_count`

其中 `visit_time_and_count` 需要是“小时 -> 次数”的字典样式字符串，因为 [POI2emb.py](F:\jjy\GNPR\GNPR-SID0608\V2\POIembedding\POI2emb.py) 会把它解析成时间特征。

如果走 `LLM4POI_data` 路线，README 明确提到需要先生成 `poi_info.csv`。

### 第三步：生成类别语义向量

[category2vec.py](F:\jjy\GNPR\GNPR-SID0608\V2\POIembedding\category2vec.py) 的实现方式是：

1. 读取 POI 表里的 `category` 列。
2. 抽取所有唯一类别。
3. 使用 `sentence-transformers` 模型对类别文本做编码，默认模型是 `all-MiniLM-L6-v2`。
4. 可选用 PCA 把向量降到指定维度，默认脚本参数中是 `64`。
5. 输出 `category_to_embedding.pkl`。

这一步本质上是把“POI 类别文本”变成一个可复用的语义嵌入字典。

### 第四步：生成 POI 连续向量表示

[POI2emb.py](F:\jjy\GNPR\GNPR-SID0608\V2\POIembedding\POI2emb.py) 会把每个 POI 编码成一个连续向量。代码里当前实际使用了 3 类特征：

1. 类别特征
   从 `category_to_embedding.pkl` 里读取类别向量。

2. 空间特征
   把经纬度转成三维球面坐标。

3. 时间特征
   从 `visit_time_and_count` 提取小时访问分布，然后构造成 Fourier 风格的周期时间特征。

最终这些向量被直接拼接，输出为 `poi_Emb_dict.pkl`，格式是：

- key: `pid`
- value: 对应 POI 的 numpy 向量

代码里还预留了 CF embedding 的位置，但当前版本已经注释掉，没有真正使用协同过滤向量。

### 第五步：训练 SID 量化模型

[train_SID.py](F:\jjy\GNPR\GNPR-SID0608\V2\SID\train_SID.py) 和 [SID_trainer.py](F:\jjy\GNPR\GNPR-SID0608\V2\SID\SID_trainer.py) 负责训练 SID 模型。

输入是上一步生成的 `poi_Emb_dict.pkl`。

数据读取方式在 [POIdatasets.py](F:\jjy\GNPR\GNPR-SID0608\V2\SID\POIdatasets.py) 中定义：

1. 加载 pickle 字典。
2. 按 `pid` 排序。
3. 取出对应向量，组成训练集。

量化模型是 `CRQVAE`，位于 `SID/CRQVAE/`。从 `V2.md` 和代码看，核心特点是：

1. 使用基于投影的 cosine similarity quantization。
2. 通过 EMA 缓解 codebook collision。
3. 多级 codebook 组合生成离散索引。

训练时会保存：

- 最佳 loss 模型
- 最低 collision rate 模型
- 若干周期性 checkpoint

当前评估指标里最重要的是 collision rate，也就是不同 POI 发生 SID 冲突的比例。

### 第六步：导出 Semantic ID

[get_SID.py](F:\jjy\GNPR\GNPR-SID0608\V2\SID\get_SID.py) 会加载训练好的最佳模型，再对全部 POI 向量做量化推理，生成最终 SID。

输出逻辑是：

1. 对每个 `pid` 生成一个多级离散索引序列。
2. 如果多个 POI 命中了同一个 SID，会在末尾额外附加一个区分编号。
3. 最后导出为 CSV，字段是：
   - `pid`
   - `sid`
   - `vector`

所以从实现角度看，这里的 SID 并不是单个整数，而是一个离散索引序列，必要时还会加一个碰撞消歧尾号。

### 第七步：构造 LLM 训练数据

这一步 README 没完全展开，但从文件命名看，完整链路应该是：

1. 利用上一步导出的 SID 文件，把原本的 POI 序列替换成 SID 序列。
2. 通过 [get_align_data.ipynb](F:\jjy\GNPR\GNPR-SID0608\V2\dataprocess\get_align_data.ipynb) 构造 SID 对齐训练数据。
3. 通过 `llm_dataprocess.ipynb` 构造标准 SFT 的 JSON 数据。

LLM 训练脚本要求的数据格式是 JSON，并且每条样本至少要有：

- `instruction`
- `input`
- `output`

因为 `LLM/train/*.py` 中的 `format_fn()` 就是按这个三段格式拼 prompt 的。

### 第八步：训练 LLM

仓库里给了两条 LLM 路线。

1. 先对齐，再 SFT
   - [align_sid.py](F:\jjy\GNPR\GNPR-SID0608\V2\LLM\train\align_sid.py)
   - [sft_after_alignment.py](F:\jjy\GNPR\GNPR-SID0608\V2\LLM\train\sft_after_alignment.py)

2. 直接 SFT
   - [sft_without_alignment.py](F:\jjy\GNPR\GNPR-SID0608\V2\LLM\train\sft_without_alignment.py)

实现方法大致是：

1. 使用 Hugging Face `AutoModelForCausalLM` 加载基础模型。
2. 使用 LoRA 做参数高效微调。
3. 用 `trl.SFTTrainer` 做监督微调。
4. 训练后可用 [merge_model.py](F:\jjy\GNPR\GNPR-SID0608\V2\LLM\train\merge_model.py) 把 LoRA 权重并回基础模型。

`align_sid.py` 的设计重点是只训练 `embed_tokens`，所以更像“先把 SID token 嵌入映射到模型词向量空间”的对齐阶段。

`sft_after_alignment.py` 和 `sft_without_alignment.py` 则是完整的生成式微调，只是 target modules 略有不同。

## 4. 各模块的实现特点

从代码看，这个项目的实现风格是“先构造可解释的 POI 语义向量，再用离散量化把连续语义映射成 SID，最后把 SID 当作 LLM 可学习的离散 token 序列”。

核心设计点有 3 个：

1. POI 语义建模不是端到端学习，而是人工拼接类别、空间、时间特征。
   优点是简单、可控、容易迁移到新数据。

2. SID 不是直接哈希，也不是普通类别 ID，而是通过量化模型学习出来的离散语义编码。
   这让相似 POI 有机会拥有结构上相近的离散 ID。

3. LLM 部分不是从原始 POI 文本直接学习，而是把轨迹序列转成 SID 序列，再做生成式推荐。
   这样可以减轻大规模 POI 文本离散化带来的词表压力。

## 5. 当前仓库缺失或不完整的内容

从“能否直接复现”这个角度检查，这个仓库还缺不少关键内容。

### 5.1 缺少环境依赖说明

仓库里没有看到：

- `requirements.txt`
- `environment.yml`
- 安装说明

但代码实际依赖至少包括：

- `torch`
- `numpy`
- `pandas`
- `transformers`
- `datasets`
- `trl`
- `peft`
- `sentence-transformers`
- `scikit-learn`
- `tqdm`
- `wandb`
- 可能还需要 `deepspeed`

这意味着别人拿到仓库后，无法直接搭环境。

### 5.2 缺少可直接运行的命令示例

很多脚本都提供了 `argparse` 参数，但主函数里默认值几乎全部是空字符串，例如：

- `--data_path`
- `--ckpt_dir`
- `--csv_path`
- `--output_dir`
- `base_model`
- `train_dataset`
- `valid_dataset`

也就是说，当前仓库更像“脚本模板”，而不是“开箱即用项目”。

### 5.3 缺少数据样例和字段说明

虽然 README 提到了 `poi_info.csv`，但仓库里没有实际样例文件，也没有正式 schema 文档。

实际至少应该补清楚：

- 每个 CSV/JSON 文件需要哪些列
- `visit_time_and_count` 的精确格式
- SID 导出 CSV 的标准格式
- LLM 训练 JSON 的字段规范

### 5.4 LLM 部分作者自己也明确说可能不完整

[V2.md](F:\jjy\GNPR\GNPR-SID0608\V2\V2.md) 里已经明确写了：

- `LLM/` 是示例实现
- 可能还存在未解决问题或不完整组件
- 更推荐使用 LLaMA-Factory

这和代码检查结果是一致的。当前 LLM 部分更适合参考，不适合作为唯一训练入口。

### 5.5 notebook 主导流程较多，但缺少串联文档

数据预处理主要放在 notebook 中，这没问题，但当前没有一份明确的“先跑哪个 notebook，再跑哪个脚本”的流程文档。

这也是本项目最缺的部分之一：流程能看懂，但执行顺序不够清晰。

### 5.6 有一些实现细节仍显粗糙

代码层面还看到一些小问题：

1. 多个脚本导入了 `fire`，但没有实际使用。
2. `category2vec.py` 里打印了 `categories.npy`、`category_embeddings.csv`，但代码没有真正保存这两个文件。
3. `CUDA_VISIBLE_DEVICES` 在部分脚本里被硬编码。
4. 部分路径变量和目录变量保留为空字符串，需要人工补全。
5. 仓库里没有推理/评估脚本，训练后如何做下一 POI 生成验证没有完整闭环。

## 6. 现在可以认为“还缺什么”

如果目标是让这个项目真正可复现，建议至少补以下几项：

1. 增加 `requirements.txt` 或 `environment.yml`。
2. 增加一个总流程文档，写清楚输入输出文件和执行顺序。
3. 给出最小数据样例。
4. 给出每个脚本的实际命令行例子。
5. 补一个评估脚本或推理脚本。
6. 清理硬编码路径、空字符串参数、未使用 import。
7. 对 LLM 训练部分说明“推荐脚本”和“不推荐脚本”的边界。

## 7. 结论

这个仓库的核心思路是清楚的，主线流程也基本完整：

原始轨迹数据 -> POI 元信息 -> POI 连续向量 -> SID 量化 -> SID 序列数据 -> LLM 训练。

但它目前更像“研究代码公开版”，不是工业化整理后的复现实验仓库。最大的短板不是算法本身，而是：

- 缺环境说明
- 缺样例数据格式
- 缺一键运行命令
- 缺评估闭环

如果你接下来要继续用这个项目，最值得先做的事不是直接跑训练，而是先把：

1. 数据格式说明
2. 环境依赖
3. 推荐执行顺序

这三件事补齐。这样后面无论你是复现论文、换自己的数据，还是迁移到别的 LLM 训练框架，都会顺很多。
